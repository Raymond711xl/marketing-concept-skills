interface:
  display_name: "Web Evidence Collector"
  short_description: "Collect traceable evidence pools"
  default_prompt: "Use $web-evidence-collector to collect sourced evidence, brand hard-data candidates, an evidence brief, and a downstream-ready evidence pool."
